# Memory Constraints (Forces disk spills)
shared_buffers = 64MB
work_mem = 4MB
maintenance_work_mem = 32MB
temp_buffers = 8MB

# Extensions
shared_preload_libraries = 'pg_stat_statements'

# Logging
log_statement = 'none'
log_duration = off
log_min_duration_statement = 1000
